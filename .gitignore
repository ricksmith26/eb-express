# Elastic Beanstalk Files
.elasticbeanstalk/*
!.elasticbeanstalk/*.cfg.yml
!.elasticbeanstalk/*.global.yml
!.elasticbeanstalk/config.yml
node_modules

.env
.env.local

# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
terraform.tfvars
*.tfplan
.terraform.lock.hcl

# SSH Keys
*.pem
*.key
id_rsa
id_rsa.pub

# OS files
.DS_Store
Thumbs.db
# Terraform
*.tfplan
terraform/*.tfplan
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/*.tfstate
terraform/*.tfstate.backup

# Old backup files
*.old

# Initial EB placeholder
terraform/initial-app/
terraform/initial-app.zip

# CA certificates (private keys)
# Note: CA key is committed for EB deployment (dev CA only)
# For production, use AWS Secrets Manager
# certs/*.key
